# Default: let Git handle text files automatically
* text=auto

# Source code - normalize to LF in the repository
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
# Scripts - normalize to LF in the repository